using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace DbSybaseService
{
    public class DbAsa9Context
    {
      /*  public ResponseMsg ejemploFuncionLeeDataDeAsa()
        {
            ResponseMsg data;
            try
            {
                //todo ok
                data = new ResponseMsg()
                {
                    status = 0,
                    msg = "Ok",
                    data = new DataTable() // aca retornas la data
                };
            }
            catch (Exception ex)
            {
                data = new ResponseMsg()
                {
                    status = 99,
                    msg = ex.Message,
                };
            }
            finally { 
                //cerrar conexion
            }

            return data;
        }
      */
    }
}
